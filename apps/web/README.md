# Deprecated

Product UI is **`../../frontend`** (Next.js Aarogya app).

Do not add marketing or auth pages here. Keep this package only if a legacy Docker path still references it; prefer `frontend/` in compose.
